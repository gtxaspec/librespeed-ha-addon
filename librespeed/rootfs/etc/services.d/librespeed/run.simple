#!/usr/bin/with-contenv bash
# ==============================================================================
# Home Assistant Add-on: LibreSpeed
# Runs the LibreSpeed server (simplified version for debugging)
# ==============================================================================

echo "Starting LibreSpeed server (simplified)..."

# Run LibreSpeed with basic configuration
exec /usr/local/bin/librespeed-rs \
    --bind-address :: \
    --listen-port 8080 \
    --assets-path /assets